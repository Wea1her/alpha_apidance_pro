# PostgreSQL 与网站作为唯一事实源

现有系统把 Telegram 和本地 JSON/JSONL 同时当作状态与展示载体，无法可靠支撑项目筛选、报告版本、战壕、台账和日历。升级后以 PostgreSQL 中的不可变原始事件、结构化业务状态和事务型 Outbox 为唯一事实源；网站是完整工作台，Telegram 只发送高优先级提醒。这个选择牺牲了本地文件的零运维优势，换取可查询、可迁移、可恢复和一致的工作流状态。
