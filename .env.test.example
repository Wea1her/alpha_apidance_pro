# 存储层测试用的本地 PostgreSQL（复制为 .env.test 后使用）
# docker run -d --name daxinjiankong-pg-test -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=daxinjiankong_test -p 55432:5432 postgres:16-alpine
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55432/daxinjiankong_test
